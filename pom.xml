<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.t1m3.qplayer</groupId>
    <artifactId>musicplayer-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>QML Music Player</name>

    <modules>
        <module>player-core</module>
        <module>desktop-host</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>8</maven.compiler.release>

        <qml4j.version>0.2.31</qml4j.version>
        <skija.version>0.143.17</skija.version>
        <gson.version>2.10.1</gson.version>
        <jaudiotagger.version>3.0.1</jaudiotagger.version>
        <xz.version>1.12</xz.version>
        <!-- Fork commit cffe3f8; CI-built for all six desktop OS/arch targets. -->
        <swingwebview.version>1.4.0-qplayer.2</swingwebview.version>
    </properties>

    <repositories>
        <repository>
            <id>qplayer-swingwebview-fork</id>
            <name>QPlayer Swing WebView fork</name>
            <url>https://raw.githubusercontent.com/TIMER-err/swingwebview/maven-repo</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.github.timer-err</groupId>
                <artifactId>qml4j-core</artifactId>
                <version>${qml4j.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>${gson.version}</version>
            </dependency>
            <dependency>
                <groupId>org.tukaani</groupId>
                <artifactId>xz</artifactId>
                <version>${xz.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
